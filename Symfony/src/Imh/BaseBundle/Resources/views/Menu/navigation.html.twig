<nav class="navigation">
    <div class="wrapper">
        {% spaceless %}
            {{ knp_menu_render('main', {'currentAsLink':false, 'allow_safe_labels': true} ) }}
        {% endspaceless %}
    </div>
</nav>